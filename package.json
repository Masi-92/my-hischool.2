{
  "name": "my-hischool",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix back-end && npm install --prefix front-end",
    "dev:back": "npm run dev --prefix back-end",
    "dev:front": "npm run dev --prefix front-end",
    "dev": "concurrently -n back,front -c blue,green \"npm run dev:back\" \"npm run dev:front\"",
    "start:back": "npm start --prefix back-end",
    "build:front": "npm run build --prefix front-end"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
